"""
This package provides scripts to automate
process of development of `selection` package.solve

You can run scripts with `run` command that is
provided by the `runscript` package.

Packages contents:

 * test - script to run `selection` tests
"""
